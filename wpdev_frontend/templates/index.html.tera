<!DOCTYPE HTML>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WPDEV - Instance Management</title>
    <script src="/static/htmx.min.js"></script>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>
<div class="loader">
    <div class="spinner"></div>
</div>
<!-- Create Instance Form -->
<div class="container wpdev-header">
    <h1>WPDEV Instance Management</h1>
    <div class="wpdev-controls">
        <button hx-post="/create_instance"
            hx-target="#instances-list"
            hx-indicator=".loader"
        >Create Instance</button>

        <button hx-delete="/delete_instances"
            hx-target="#instances-list"
            hx-confirm="Are you sure you want to delete all instances?"
            hx-indicator=".loader"
        >Delete All</button>
        <button hx-post="/start_all_instances"
            hx-target="#instances-list"
            hx-indicator=".loader"
        >Start All</button>
        <button hx-post="/stop_all_instances"
            hx-target="#instances-list"
            hx-indicator=".loader"
        >Stop All</button>
        <button hx-post="/restart_all_instances"
            hx-target="#instances-list"
            hx-indicator=".loader"
        >Restart All</button>
    </div>
</div>

<div class="container">
    <h2>Instances</h2>
    <div id="instances-list" hx-get="/list_all_instances" hx-trigger="load">
        <!-- Dynamic content from instances.html.tera will be loaded here -->
    </div>
</div>

</body>
</html>

